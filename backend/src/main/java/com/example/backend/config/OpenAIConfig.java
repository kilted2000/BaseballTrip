package com.example.backend.config;

import org.springframework.context.annotation.Configuration;

/**
 * Placeholder OpenAI configuration.
 * The `openai-java-spring-boot-starter` provides its own auto-configuration.
 * Keep this class to document config and avoid manual wiring of unavailable SDK classes.
 */
@Configuration
public class OpenAIConfig {

    // No manual OpenAI bean defined here; use the starter's auto-configured client if needed.

}







